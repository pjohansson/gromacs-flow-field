#ifndef PLUGIN_SHEAR_COUPLING_ENUM_H
#define PLUGIN_SHEAR_COUPLING_ENUM_H

#include <cstdlib>

// enum class SwapStrategy {
//     Edges,                  /* Area 0 = bottom edge, area 1 = top edge */
//     EdgeCenter,             /* Area 0 = top and bottom edges (halved), area 1 = middle of box */
//     Num
// };

// const char* eShearCouplStrategy_names[static_cast<size_t>(SwapStrategy::Num) + 1];
// const char* eShearCouplStrategy_names[3] 
//     = { "Edges", "Edge-Center", nullptr };

#endif // PLUGIN_SHEAR_COUPLING_ENUM_H